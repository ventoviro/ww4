language: php

sudo: true
dist: trusty
group: edge

php:
  - hhvm-3.15

#addons:
#  postgresql: "9.3"
#
#services:
#  - memcached # will start memcached

before_install:
  - composer self-update
  - if [[ ${TRAVIS_PHP_VERSION} == "hhvm-3.15" ]]; then mkdir -p /home/travis/.phpenv/versions/hhvm/etc/conf.d; phpenv config-add build/config/hhvm.ini; fi
#  - sh -e build/travis/scripts/apt-get.sh
#  - sh -e build/travis/scripts/apache2-vhost.sh
#  - sh -e build/travis/scripts/apache2-configure.sh

before_script:
  - composer install --ignore-platform-reqs --no-interaction
#  - phpenv rehash

script:
  - phpunit --configuration phpunit.travis.xml
